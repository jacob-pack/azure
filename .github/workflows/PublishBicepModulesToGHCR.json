name: publish bicep modules to github container registry

on:
    workflow_dispatch:
        push:
            paths:
                - 'Azure/Bicep/Infrastructure/Resources/**.bicep'
jobs:
    publish:
        runs-on: ubuntu-latest

        steps:
            - name: checkout repo
              uses: actions/